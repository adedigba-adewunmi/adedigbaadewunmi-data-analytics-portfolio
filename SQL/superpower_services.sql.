-- creating new table --

create database SUPERPOWER_SERVICES_SUPPORT;
USE SUPERPOWER_SERVICES_SUPPORT;
 create table CUSTOMERS
(CUSTOMER_ID int auto_increment primary key,CUSTOMER_NAME varchar(100) ,PRODUCT varchar(100),QUANTITY int,UNIT_PRICE decimal(10,2),SALE_DATE date )
INSERT INTO CUSTOMERS (CUSTOMER_NAME,PRODUCT,QUANTITY,UNIT_PRICE,SALE_DATE) VALUES ('JARE SOLAR',' JINKO 650WATT PANEL',70,130000,'2026-06-25'),
('BRIGHT ENERGY',' 6.5KVA ANERN INVERTER',7,650000,'2026-06-26'),
('KING SOLAR','TUBULAR BATTERY',12,150000,'2026-06-27'),
('DIVINE TECH',' 15KWH JSD BATTERY',8,2500000,'2026-06-28'); 
 
SELECT*
FROM CUSTOMERS;
ALTER TABLE customers
add TOTAL_PRICE decimal(12,2);

select*
from customers;

update customers
SET TOTAL_PRICE = QUANTITY*UNIT_PRICE
WHERE CUSTOMER_ID > 0;
SELECT*
FROM CUSTOMERS;
 
Select CUSTOMER_ID ,CUSTOMER_NAME , PRODUCT
FROM CUSTOMERS;

SELECT*
FROM CUSTOMERS
WHERE TOTAL_PRICE > 5000000; 

update CUSTOMERS
SET CUSTOMER_NAME='JARE SOLAR LIMITED'
WHERE CUSTOMER_ID=1;

SELECT*
FROM CUSTOMERS;

 SELECT CUSTOMER_NAME AS CLIENT
 FROM CUSTOMERS;

SELECT CUSTOMER_NAME,TOTAL_PRICE,
TOTAL_PRICE+10000000 AS NEW_PRICE
FROM customers;

select lower(CUSTOMER_NAME)
from customers;

create VIEW TOP_CUSTOMER AS
select
UNIT_PRICE
FROM CUSTOMERS
WHERE total_price<12000000;
 
 select*
 FROM TOP_CUSTOMER;


